# 폴더 없는 버전 — 이렇게 올리세요

이 폴더의 파일은 **전부 같은 층에 있고 하위 폴더가 없습니다.**
CSS · 자바스크립트 · 사진이 모두 `index.html` 안에 들어가 있어서,
파일만 올리면 디자인이 그대로 나옵니다.

## 올리는 순서

1. GitHub 저장소로 이동
2. 기존에 올라간 `index.html`, `privacy.html`, `404.html`이 있다면 그대로 두세요 (덮어쓰기 됩니다)
3. **Add file → Upload files** 클릭
4. 이 폴더를 열어서 **안에 있는 파일 8개를 전부 선택**(Ctrl+A)하고 드래그
   - `index.html`
   - `privacy.html`
   - `404.html`
   - `og-image.png`
   - `CNAME`
   - `robots.txt`
   - `sitemap.xml`
   - `.nojekyll`
5. 아래 **Commit changes** 클릭
6. 1~2분 뒤 `https://pyoseontax.com` 에서 `Ctrl+Shift+R`(맥은 `Cmd+Shift+R`)로 강력 새로고침

`.nojekyll`이 숨김 파일이라 보이지 않으면, Windows 탐색기 **보기 → 숨긴 항목** 체크,
맥 Finder는 `Cmd+Shift+.` 를 누르면 나타납니다. 이 파일이 빠져도 동작에는 문제없습니다.

## 이전에 올린 assets 폴더가 있다면

지금 이 버전은 `assets` 폴더가 필요 없습니다.
저장소에 `assets` 폴더가 일부만 올라가 있어도 무시되니 그대로 두셔도 되고,
정리하고 싶으면 삭제하셔도 됩니다.

## 내용을 수정할 때

`index.html`을 GitHub에서 열고 연필(✏️) 아이콘으로 수정하면 됩니다.
파일 안에 사진이 긴 문자열(`data:image/png;base64,...`)로 들어가 있어 스크롤이 길지만,
글자를 고칠 때는 `Ctrl+F`로 원하는 문구를 찾아 바꾸시면 됩니다.

찾기 쉬운 위치:

| 고칠 내용 | 검색어 |
|---|---|
| 상담 신청 폼 주소 | `YOUR_FORM_ID` |
| 상담 시간 | `09:00` |
| 전화번호 | `8287` |
| 주차 안내 | `무료주차` |

## 사진을 바꾸고 싶을 때

사진이 파일 안에 박혀 있어서 직접 교체하기 어렵습니다.
바꾸실 사진을 저에게 주시면 새 `index.html`을 만들어 드리는 편이 빠릅니다.
